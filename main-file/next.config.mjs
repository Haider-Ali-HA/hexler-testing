/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['plus.unsplash.com', 'images.pexels.com', 'unsplash.com','res.cloudinary.com'],
    },
};

export default nextConfig;
